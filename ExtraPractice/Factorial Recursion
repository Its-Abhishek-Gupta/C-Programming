#include<stdio.h>
int fact(int);
int main()
{
    int n,f;
    printf("Enter the number to find the factorial:");
    scanf("%d",&n);
    f=fact(n);
    printf("Factorial of the given number is:%d",f);
    return 0;
}
int fact(int x){
    if (x==0)
        return 1;
    return(x*fact(x-1));
}
