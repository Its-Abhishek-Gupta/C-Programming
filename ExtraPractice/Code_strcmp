#include<stdio.h>
int main()
{
    char a[100],b[100];
    int i=0,j=0,l;
    printf("Enter the string a:");
    scanf("%s",a);
    printf("Enter the string b:");
    scanf("%s",b);
    while (a[j]!=0 && b[j]!=0){
        if (a[j]!=b[j]){
            i=(int) a[j]-b[j];
            printf("i=%d",i);
            break;}
        else
            i=0;
        j++;
    }
    if (i==0)
        printf("\nStrings are equal:");
    else
        printf("\nStrings not equal");
    return 0;
}
