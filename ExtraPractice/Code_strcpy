#include<stdio.h>
int main()
{
    char a[100],b[100];
    int i=0;
    printf("enter the string a:");
    scanf("%s",a);
    while (a[i]!='\0'){
        b[i]=a[i];
        i++;
    }
    b[i]='\0';
    printf("The copied string is: %s",b);
    return 0;
}
